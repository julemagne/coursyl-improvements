<div class="hero-unit">
  <h1>Courses</h1>
  <p>A student-centric window into your classes, assignments, lectures, and grades.</p>
</div>


<div class="row">
  <div class="span4">
    <h2>Why use Courses?</h2>
    <p>Okay, it's true: you may not have a choice.  Other than that, you might also use Courses because you can:</p>
    <ul>
      <li>See your current grade</li>
      <li>See your final grade if you ace everything else</li>
      <li>See your final grade if you move to Maui now</li>
      <li>View upcoming lessons in a cool topical tree</li>
      <li>Submit assignments and get grades fast</li>
      <li>Watch lectures, review slides, and catch up on what you may have missed</li>
    </ul>
    <p><a class="btn" href="/users/sign_up">Sign Up &raquo;</a></p>
  </div>

  <div class="span4">
    <h2>Example Classes</h2>
    <p>Here are some of the classes that use Courses:</p>
    <ul>
      <% Course.example_courses.each do |c| %>
        <li><a href="<%= course_path(c) %>"><%= c.code_and_name %></a></li>
      <% end %>
    </ul>
  </div>

  <div class="span4">
    <h2>Sign In</h2>
    <%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
      <div class="input-prepend">
        <span class="add-on">@</span>
        <%= f.email_field :email, required: true, autofocus: true, placeholder: "Email Address" %>
      </div>

      <div class="input-prepend">
        <span class="add-on"><i class="icon-key"></i></span>
        <%= f.password_field :password, required: true, placeholder: "Password" %>
      </div>

      <% if devise_mapping.rememberable? -%>
        <div class="inline-check-box"><%= f.check_box :remember_me %> <%= f.label :remember_me %></div>
      <% end -%>

      <div><%= f.submit "Sign in &raquo;".html_safe, class: "btn" %></div>
    <% end %>

    <%= render "devise/shared/links" %>
  </div>
</div>

