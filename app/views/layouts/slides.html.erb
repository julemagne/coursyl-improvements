<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= (content_for?(:title) ? yield(:title) : "Coursyl") + (Rails.env.development? ? " - DEVELOPMENT" : "") %></title>
    <%= favicon_link_tag 'favicon.ico' %>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Coursyl" %>">
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->
    <%= javascript_include_tag "reveal" %>
    <%= stylesheet_link_tag "reveal" %>
    <%= stylesheet_link_tag "reveal-theme/moon" %>
    <%= stylesheet_link_tag "slides" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div class="reveal">
      <div class="slides">
        <%= render 'layouts/messages' %>
        <%= yield %>
      </div>
    </div>
    <script>
      // Full list of configuration options available here:
      // https://github.com/hakimel/reveal.js#configuration
      Reveal.initialize({
        controls: true,
        progress: true,
        history: true,
        center: true,

        theme: Reveal.getQueryHash().theme, // available themes are in /css/theme
        transition: Reveal.getQueryHash().transition || 'default', // default/cube/page/concave/zoom/linear/fade/none
      });

    </script>
  </body>
</html>