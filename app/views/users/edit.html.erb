<% if current_user.admin %>
  <p><%= link_to back_arrow + "Back to User List", users_path %></p>
<% else %>
  <p><%= link_to back_arrow + "Back to Home", home_index_path %></p>
<% end %>

<h2>User Details</h2>

<%= render 'form' %>
